# AI CAD — C/S 架构方案

> 版本：v1.0 | 日期：2026-06-09  
> 从 BS（浏览器）架构迁移到 CS（桌面客户端）架构的完整方案

---

## 一、背景与动机

### 现有 BS 架构的痛点

当前系统基于浏览器（已删除）运行，存在以下问题：

1. **预览与产出分离** — 看的是 SVG 示意图，最终 CAD 画图走的是另一条 pywin32 COM 链路，两套渲染逻辑不完全一致
2. **交互割裂** — 浏览器对话 → 导出 DXF → 打开 AutoCAD → 发现问题 → 回浏览器改，多窗口切换低效
3. **边界配置弱** — 网页表单承载不了复杂的行业规则编辑、拖拽排序、约束冲突可视化
4. **缺乏实时反馈** — HTTP 轮询等待 AI 生成，看不到中间过程

### CS 架构优势

- 预览 = 最终图纸，同一份渲染逻辑
- 一键推 CAD，不需要中间导出
- 原生桌面 UI，边界配置可做到树形编辑、拖放排序
- 本地缓存版本数据，断网也能翻历史
- 可扩展 WebSocket 实时流对话

---

## 二、总体架构

```
┌──────────────────────────────────────────┐
│         桌面客户端 (Python + PySide6)       │
│                                          │
│  ┌────────────────────────────────────┐  │
│  │  服务器设置 (IP/Port 可配置)          │  │
│  │  登录 / Token 持久化                │  │
│  ├────────────────────────────────────┤  │
│  │  项目列表 + 新建                     │  │
│  │  设计边界多选 + 自定义规则编辑         │  │
│  │  参考项目选择                        │  │
│  ├────────────────────────────────────┤  │
│  │  AI 对话面板                        │  │
│  │  多会话切换                          │  │
│  │  版本管理（树形、对比、回滚）           │  │
│  ├────────────────────────────────────┤  │
│  │  本地 2D 预览 (QGraphicsView)        │  │
│  │  → 与推 CAD 共用同一套坐标计算         │  │
│  │  → 所见即所得                        │  │
│  ├────────────────────────────────────┤  │
│  │  "推到 CAD" → AutoCAD COM 直绘       │  │
│  │  → 自动保存 .dwg 到本地              │  │
│  │  → 自动上传 DWG 到服务端存档           │  │
│  └────────────────────────────────────┘  │
│              │ HTTP / WebSocket            │
└──────────────┼────────────────────────────┘
               │
┌──────────────┴────────────────────────────┐
│          AI 服务器 (Python FastAPI)         │
│                                           │
│  ┌─────────────────────────────────────┐  │
│  │  JWT 认证 + 权限控制                  │  │
│  │  项目管理 (CRUD)                     │  │
│  │  会话/版本树存储                      │  │
│  │  对话记录持久化                       │  │
│  │  DeepSeek API 集成                   │  │
│  ├─────────────────────────────────────┤  │
│  │  DWG 文件管理                        │  │
│  │  → 上传/下载                         │  │
│  │  → 历史扫描（含已删除版本）             │  │
│  │  → 文件永久保留，不随数据库记录删除     │  │
│  └─────────────────────────────────────┘  │
│                                           │
└───────────────────────────────────────────┘
               │ COM / ActiveX
┌──────────────┴────────────────────────────┐
│              AutoCAD                      │
│  → 纯输出端，接收客户端的图元绘制指令        │
└───────────────────────────────────────────┘
```

---

## 三、客户端技术选型

| 技术 | 选型 | 理由 |
|------|------|------|
| UI 框架 | **PySide6** (Qt for Python) | 原生桌面控件、QGraphicsView 2D 引擎、跨平台、与后端同语言 |
| HTTP 客户端 | **httpx** | 已在后端使用，复用经验 |
| AutoCAD 通信 | **pywin32 COM** | 复用现有 bridge 逻辑，直接控制 AutoCAD 图元 |
| 配置持久化 | JSON 文件 (`%APPDATA%/hsxb-ai-cad/`) | 轻量，免数据库 |
| 打包 | **PyInstaller** → 单文件 .exe | 设计师双击即用，无需装 Python |

---

## 四、核心功能模块

### 4.1 登录与认证

- 首次启动弹出服务器设置窗口（IP + 端口 + 测试连接）
- 登录后 Token 持久化到本地，下次自动登录
- Token 过期自动弹出登录窗口

### 4.2 项目管理

- 项目列表（左栏），含会话数量和更新时间
- 新建项目：填写名称 → 多选设计边界 → 可选参考项目 → 自动创建默认会话
- 删除项目：数据库记录清除，服务器端 DWG 文件保留

### 4.3 设计边界配置（未来迭代）

- 当前：多选已有边界模板
- 计划：边界规则树形编辑器、拖拽排序、约束冲突可视化、自定义规则语法高亮

### 4.4 AI 对话面板

- 中央对话区，支持 Markdown 渲染
- 底部输入框，Enter 发送
- 后台线程异步等待 AI 响应，不阻塞 UI
- 计划升级：WebSocket 流式对话，边想边显示

### 4.5 版本管理

- 右侧版本树（v1 → v2 → v3）
- 每个版本显示编号、描述、日期
- 计划：版本叠图对比（新旧版本半透明叠加）

### 4.6 本地预览 (QGraphicsView)

- 客户端直接解析 JSON → QGraphicsView 渲染
- 与推 CAD 共用同一套坐标计算，预览即所见
- 支持图层管理、缩放平移、图元选中
- 替代现有 SVG 预览方案

### 4.7 推送到 AutoCAD

- 点击版本旁"推 CAD"按钮
- COM 连接 AutoCAD → 新建文档 → 逐图元绘制
- 自动保存 .dwg 到本地临时目录
- 后台异步上传 DWG 到服务器

---

## 五、服务端改动

### 已有功能（无需改动）

- JWT 认证、项目管理、会话/版本、AI 对话、边界管理、大模型配置

### 新增功能

| 功能 | 接口 | 说明 |
|------|------|------|
| DWG 上传 | `PUT /api/versions/{id}/dwg` | multipart 上传 |
| DWG 下载 | `GET /api/versions/{id}/dwg` | 文件流下载 |
| DWG 历史扫描 | `GET /api/projects/{id}/dwgs` | 扫描文件系统，含已删除版本 |
| 数据库字段 | `versions.dwg_file_path` | 记录 DWG 存储路径 |

### 设计原则

- DWG 文件独立于数据库生命周期：删除记录不删除文件
- 按 `项目ID/会话ID/v{版本号}-{版本ID}.dwg` 组织目录结构

### 计划优化

| 优化项 | 说明 | 优先级 |
|--------|------|--------|
| WebSocket 实时流 | AI 边生成边推送到客户端 | 高 |
| 统一 JSON Schema | 约束 AI 输出格式（zones/rooms 等） | 中 |
| 预览接口废弃 | 客户端自绘后 SVG 预览可下线 | 低 |

---

## 六、开发与部署流程

### 开发环境

- macOS 上编写和调试 PySide6 UI（非 CAD 功能）
- Windows 上测试 AutoCAD COM 和打包

### 代码同步

- 简单方式：zip 打包整个项目目录，U 盘/共享文件夹拷贝
- 推荐方式：Git 仓库，两边 pull

### 启动命令

```powershell
# 服务端（服务器电脑）
cd server
pip install -r requirements.txt python-multipart
uvicorn app.main:app --host 0.0.0.0 --port 3000

# 客户端（设计师电脑）
cd client
pip install -r requirements.txt
python -m app.main
```

### 打包交付

```powershell
cd client
pip install pyinstaller
pyinstaller app.spec
# 输出：dist/AICAD.exe
# 复制到设计师电脑，双击即用
```

---

## 七、数据流对比

### 旧 BS 链路

```
浏览器输入 → HTTP POST → FastAPI → DeepSeek
                                      │
               ┌──────────────────────┘
               ▼
         JSON 存入数据库
               │
   ┌───────────┼───────────┐
   ▼           ▼           ▼
  SVG预览    DXF导出    桥梁Flask
  (示意图)   (polyline)  → pywin32 → AutoCAD
```

### 新 CS 链路

```
客户端输入 → HTTP/WS → FastAPI → DeepSeek
                           │
              ┌────────────┘
              ▼
        JSON 存入数据库
              │
              ▼
       客户端 QGraphicsView 渲染（原生矢量预览）
              │
              ▼
       同一份数据 → AutoCAD COM 直接绘图
              │
              ▼
       自动上传 DWG 到服务器存档
```

---

## 八、版本规划

| 阶段 | 内容 | 状态 |
|------|------|------|
| v0.1 | 网页原型 已删除 | ✅ 完成 |
| v1.0 | 服务端 FastAPI 完整功能 | ✅ 完成 |
| v1.5 | DWG 存档 + 预览优化 | ✅ 完成 |
| v2.0 | 桌面客户端最小可用（登录+对话+推CAD） | 🔧 代码完成，待测试 |
| v2.1 | QGraphicsView 原生预览 | 📋 计划中 |
| v2.2 | WebSocket 实时流对话 | 📋 计划中 |
| v2.3 | 版本叠图对比 | 📋 计划中 |
| v3.0 | 边界规则高级编辑器 | 📋 计划中 |

---

## 九、相关文件

| 文件 | 说明 |
|------|------|
| `README.md` | 项目说明与快速开始 |
| `STATUS.md` | 开发状态跟踪 |
| `HANDOFF.md` | 开发移交记录（换电脑参考） |
| `AI-CAD-桥梁服务方案.md` | 原始详细设计文档 |
